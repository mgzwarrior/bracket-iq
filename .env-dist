# Django settings
DEBUG=True
SECRET_KEY=your-secret-key-here
ALLOWED_HOSTS=localhost,127.0.0.1
INTERNAL_IPS=127.0.0.1

# Database settings
DB_ENGINE=django.db.backends.postgresql
DB_NAME=bracket_iq
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=db  # Will be overridden to 'localhost' for local development
DB_PORT=5432

# Docker development workflow
# Options: full (all services), deps (only dependencies), none (local development)
DEV_DOCKER_WORKFLOW=full  # Will be overridden to 'none' for local development

# Development settings
DJANGO_SETTINGS_MODULE=bracket_iq.settings
STATIC_ROOT=static
MEDIA_ROOT=media
STATIC_URL=/static/
MEDIA_URL=/media/ 
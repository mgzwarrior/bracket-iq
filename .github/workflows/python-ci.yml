name: Python CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.9]

    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        if [ -f requirements-dev.txt ]; then pip install -r requirements-dev.txt; else pip install -r requirements.txt; fi
        
    - name: Django system checks
      run: |
        python manage.py check
        
    - name: Format with black
      run: |
        black bracket_iq
        
    - name: Basic lint with flake8
      run: |
        flake8 bracket_iq --count --select=E9,F63,F7,F82 --show-source --statistics
    
    # Temporarily disabling these checks until we've fixed all the issues    
    # - name: Lint with pylint
    #  run: |
    #    pylint --rcfile=setup.cfg bracket_iq
        
    # - name: Type check with mypy
    #  run: |
    #    mypy .
        
    # - name: Run tests
    #  run: |
    #    python manage.py test 
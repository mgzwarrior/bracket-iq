# Django settings
DEBUG=1
SECRET_KEY=your-secret-key-here
ALLOWED_HOSTS=localhost,127.0.0.1

# Database settings
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/bracket_iq

# Docker development workflow
# Options: full (all services), deps (only dependencies), none (local development)
DEV_DOCKER_WORKFLOW=none

# Development settings
DJANGO_SETTINGS_MODULE=bracket_iq.settings

# PostgreSQL settings
POSTGRES_HOST=localhost
POSTGRES_DB=bracket_iq
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5432 